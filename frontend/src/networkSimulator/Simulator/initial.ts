import { Edge, Node } from 'react-flow-renderer';
import { CustomNode } from '../store';

export const initialEdges = [] as Edge[];
export const initialNodes = [] as CustomNode[];
export const initialLans = [] as CustomNode[];
